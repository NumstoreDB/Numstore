/// Copyright 2026 Theo Lincke
///
/// Licensed under the Apache License, Version 2.0 (the "License");
/// you may not use this file except in compliance with the License.
/// You may obtain a copy of the License at
///
///     http://www.apache.org/licenses/LICENSE-2.0
///
/// Unless required by applicable law or agreed to in writing, software
/// distributed under the License is distributed on an "AS IS" BASIS,
/// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
/// See the License for the specific language governing permissions and
/// limitations under the License.

// AUTO GENERATED USING CMAKE - DO NOT MODIFY

#pragma once

#include "c_specx.h"

TEST_SUITE (numstore, 269);

HEADER_FUNC void
register_tests (void)
{
  REGISTER (numstore, block_insert_read);
  REGISTER (numstore, block_insert_remove_read);
  REGISTER (numstore, block_insert_write_read);
  REGISTER (numstore, block_random);
  REGISTER (numstore, ba_memcpy_from_basic);
  REGISTER (numstore, ba_memcpy_to_basic);
  REGISTER (numstore, cbuffer_isempty);
  REGISTER (numstore, cbuffer_len);
  REGISTER (numstore, cbuffer_avail);
  REGISTER (numstore, cbuffer_get_next_data_bytes);
  REGISTER (numstore, cbuffer_get_nbytes);
  REGISTER (numstore, cbuffer_fakewrite);
  REGISTER (numstore, cbuffer_fakeread);
  REGISTER (numstore, cbuffer_read);
  REGISTER (numstore, cbuffer_copy);
  REGISTER (numstore, cbuffer_write);
  REGISTER (numstore, cbuffer_cbuffer_move);
  REGISTER (numstore, cbuffer_cbuffer_copy);
  REGISTER (numstore, cbuffer_get_no_check);
  REGISTER (numstore, cbuffer_get);
  REGISTER (numstore, cbuffer_peek_back);
  REGISTER (numstore, cbuffer_peek_front);
  REGISTER (numstore, cbuffer_push_back);
  REGISTER (numstore, cbuffer_push_front);
  REGISTER (numstore, cbuffer_pop_back);
  REGISTER (numstore, cbuffer_pop_front);
  REGISTER (numstore, checksum_execute_simple);
  REGISTER (numstore, checksum_execute_deterministic);
  REGISTER (numstore, checksum_execute_incremental);
  REGISTER (numstore, f16_to_f32_normals_and_specials);
  REGISTER (numstore, f16_to_f32_nan_is_nan);
  REGISTER (numstore, f16_to_f32_smallest_subnormal_correct_value);
  REGISTER (numstore, parse_i32_boundary_values);
  REGISTER (numstore, parse_i64_boundary_values);
  REGISTER (numstore, ext_array_capacity_doubles_on_growth);
  REGISTER (numstore, ext_array_remove_all_produces_empty);
  REGISTER (numstore, llist_append_maintaififo_order);
  REGISTER (numstore, llist_find_returnode_and_index);
  REGISTER (numstore, llist_remove_from_head_middle_tail);
  REGISTER (numstore, llist_remove_absent_node_is_noop);
  REGISTER (numstore, checksum_known_crc32c_vector);
  REGISTER (numstore, checksum_distinct_bytes_differ);
  REGISTER (numstore, serializer_write_at_capacity_then_overflow);
  REGISTER (numstore, serializer_incremental_write_overflow);
  REGISTER (numstore, stride_constructors_resolve_correctly);
  REGISTER (numstore, string_ordering_operators);
  REGISTER (numstore, line_length_newline_found);
  REGISTER (numstore, string_equal_cases);
  REGISTER (numstore, strings_are_disjoint_cases);
  REGISTER (numstore, string_plus_concatenates);
  REGISTER (numstore, cbuffer_discard_all_resets_state);
  REGISTER (numstore, cbuffer_read_write_wraparound);
  REGISTER (numstore, cbuffer_cbuffer_move_transfers_bytes);
  REGISTER (numstore, dblb_create_basic);
  REGISTER (numstore, dblb_append_single);
  REGISTER (numstore, dblb_append_multiple);
  REGISTER (numstore, dblb_append_triggers_realloc);
  REGISTER (numstore, dblb_append_alloc_basic);
  REGISTER (numstore, dblb_append_alloc_sequential);
  REGISTER (numstore, dblb_append_alloc_triggers_realloc);
  REGISTER (numstore, dblb_different_element_sizes);
  REGISTER (numstore, dblb_struct_elements);
  REGISTER (numstore, dblb_free_resets);
  REGISTER (numstore, dblb_large_append);
  REGISTER (numstore, ext_array_insert_read);
  REGISTER (numstore, ext_array_write);
  REGISTER (numstore, ext_array_remove);
  REGISTER (numstore, ext_array_random);
  REGISTER (numstore, file_basename);
  REGISTER (numstore, gr_lock_basic_sanity);
  REGISTER (numstore, gr_lock_is_is_compatible);
  REGISTER (numstore, gr_lock_high_pressure_random);
  REGISTER (numstore, htable);
  REGISTER (numstore, fnv1a_hash_empty);
  REGISTER (numstore, fnv1a_hash_single_char);
  REGISTER (numstore, fnv1a_hash_known_value);
  REGISTER (numstore, fnv1a_hash_deterministic);
  REGISTER (numstore, lalloc_edge_cases);
  REGISTER (numstore, latch);
  REGISTER (numstore, llist);
  REGISTER (numstore, i_malloc_injection);
  REGISTER (numstore, i_realloc_basic);
  REGISTER (numstore, i_realloc_right);
  REGISTER (numstore, i_realloc_left);
  REGISTER (numstore, i_crealloc_right);
  REGISTER (numstore, i_crealloc_left);
  REGISTER (numstore, parse_i32_expect);
  REGISTER (numstore, parse_f32_expect);
  REGISTER (numstore, py_mod_f32);
  REGISTER (numstore, py_mod_i32);
  REGISTER (numstore, randu32);
  REGISTER (numstore, randu32r);
  REGISTER (numstore, randi32r);
  REGISTER (numstore, randu64r);
  REGISTER (numstore, randu64e);
  REGISTER (numstore, randi64r);
  REGISTER (numstore, randi64e);
  REGISTER (numstore, ht_insert_idx_regression_trigger_swap);
  REGISTER (numstore, robin_hood_ht);
  REGISTER (numstore, slab_alloc_simple);
  REGISTER (numstore, slab_alloc_cap_one);
  REGISTER (numstore, slab_alloc_no_duplicates);
  REGISTER (numstore, slab_alloc_free_all_realloc);
  REGISTER (numstore, slab_alloc_interleaved_patterns);
  REGISTER (numstore, slab_alloc_free_head_slab);
  REGISTER (numstore, slab_alloc_free_middle_slab);
  REGISTER (numstore, slab_alloc_minimum_size);
  REGISTER (numstore, slab_alloc_stress_random);
  REGISTER (numstore, stride_resolve);
  REGISTER (numstore, strings_all_unique);
  REGISTER (numstore, string_contains);
  REGISTER (numstore, test_mark_works);
  REGISTER (numstore, test_mark_match);
  REGISTER (numstore, compile_user_stride_basic);
  REGISTER (numstore, compile_type_primitives);
  REGISTER (numstore, compile_type_sarray);
  REGISTER (numstore, compile_type_struct);
  REGISTER (numstore, compile_type_union);
  REGISTER (numstore, compile_type_complex);
  REGISTER (numstore, dpgt_open);
  REGISTER (numstore, dpgt_merge_into);
  REGISTER (numstore, dpgt_min_rec_lsn);
  REGISTER (numstore, dpgt_exists);
  REGISTER (numstore, dpgt_add);
  REGISTER (numstore, dpgt_get);
  REGISTER (numstore, dpgt_remove);
  REGISTER (numstore, dpgt_serialize);
  REGISTER (numstore, dpgt_equal);
  REGISTER (numstore, dpgt_concurrent);
  REGISTER (numstore, fpgr_open);
  REGISTER (numstore, fpgr_new);
  REGISTER (numstore, fpgr_read_write);
  REGISTER (numstore, kvt_list_builder);
  REGISTER (numstore, lexer_two_char_tokens);
  REGISTER (numstore, lexer_single_char_operators);
  REGISTER (numstore, lexer_strings);
  REGISTER (numstore, lexer_identifiers);
  REGISTER (numstore, lexer_numbers);
  REGISTER (numstore, lexer_keywords);
  REGISTER (numstore, lexer_primitives);
  REGISTER (numstore, lexer_whitespace_handling);
  REGISTER (numstore, lexer_complex_expression);
  REGISTER (numstore, lexer_keyword_prefix);
  REGISTER (numstore, lexer_errors);
  REGISTER (numstore, lexer_empty_string);
  REGISTER (numstore, lexer_numbers_in_sequence);
  REGISTER (numstore, nupd_init);
  REGISTER (numstore, nupd_append_right);
  REGISTER (numstore, nupd_append_left);
  REGISTER (numstore, nupd_append_tip_right);
  REGISTER (numstore, nupd_append_tip_left);
  REGISTER (numstore, nupd_consume_right);
  REGISTER (numstore, nupd_consume_left);
  REGISTER (numstore, nupd_done_observing_left);
  REGISTER (numstore, nupd_done_observing_right);
  REGISTER (numstore, nupd_done_consuming_left);
  REGISTER (numstore, nupd_done_consuming_right);
  REGISTER (numstore, nupd_done_left);
  REGISTER (numstore, nupd_done_right);
  REGISTER (numstore, build_page_tree);
  REGISTER (numstore, pager_fill_ht);
  REGISTER (numstore, wal_int);
  REGISTER (numstore, pgr_close_success);
  REGISTER (numstore, pgr_delete);
  REGISTER (numstore, pgr_get_invalid_checksum);
  REGISTER (numstore, pgr_new_get_save);
  REGISTER (numstore, pager_open);
  REGISTER (numstore, pgr_open_basic);
  REGISTER (numstore, pgr_reserve_and_ctrl_lock_st);
  REGISTER (numstore, aries_rollback_basic);
  REGISTER (numstore, aries_rollback_multiple_updates);
  REGISTER (numstore, aries_rollback_with_crash_recovery);
  REGISTER (numstore, aries_rollback_clr_not_undone);
  REGISTER (numstore, dl_validate);
  REGISTER (numstore, dl_set_get);
  REGISTER (numstore, dl_read);
  REGISTER (numstore, dl_read_out_from);
  REGISTER (numstore, dl_append);
  REGISTER (numstore, dl_write);
  REGISTER (numstore, dl_memset);
  REGISTER (numstore, dl_move_left);
  REGISTER (numstore, dl_shift_right);
  REGISTER (numstore, dl_move_right);
  REGISTER (numstore, in_validate_for_db);
  REGISTER (numstore, in_set_get_simple);
  REGISTER (numstore, in_push_end);
  REGISTER (numstore, in_memcpy);
  REGISTER (numstore, in_move_left);
  REGISTER (numstore, in_move_left_two_keys);
  REGISTER (numstore, in_move_left_all_keys);
  REGISTER (numstore, in_move_left_into_empty);
  REGISTER (numstore, in_push_left);
  REGISTER (numstore, in_push_left_into_empty);
  REGISTER (numstore, in_push_left_to_full);
  REGISTER (numstore, in_move_right);
  REGISTER (numstore, in_move_right_two_keys);
  REGISTER (numstore, in_move_right_all_keys);
  REGISTER (numstore, in_move_right_into_empty_right);
  REGISTER (numstore, in_choose_lidx);
  REGISTER (numstore, in_cut_left);
  REGISTER (numstore, in_cut_left_all_at_once);
  REGISTER (numstore, in_cut_left_from_empty);
  REGISTER (numstore, in_cut_left_to_one);
  REGISTER (numstore, page_set_get_simple);
  REGISTER (numstore, vp_init_empty);
  REGISTER (numstore, vp_validate);
  REGISTER (numstore, vt_init_empty);
  REGISTER (numstore, vt_validate);
  REGISTER (numstore, prim_t_validate);
  REGISTER (numstore, prim_t_snprintf);
  REGISTER (numstore, prim_t_byte_size);
  REGISTER (numstore, prim_t_serialize);
  REGISTER (numstore, prim_t_deserialize);
  REGISTER (numstore, prim_t_random);
  REGISTER (numstore, dlgt_balance_with_prev);
  REGISTER (numstore, dlgt_balance_with_next);
  REGISTER (numstore, sarray_t_snprintf);
  REGISTER (numstore, sarray_t_byte_size);
  REGISTER (numstore, sarray_t_get_serial_size);
  REGISTER (numstore, sarray_t_serialize);
  REGISTER (numstore, sarray_t_deserialize_green_path);
  REGISTER (numstore, sarray_t_deserialize_red_path);
  REGISTER (numstore, sarray_builder);
  REGISTER (numstore, struct_t_snprintf);
  REGISTER (numstore, struct_t_byte_size);
  REGISTER (numstore, struct_t_get_serial_size);
  REGISTER (numstore, struct_t_serialize);
  REGISTER (numstore, struct_t_deserialize_green_path);
  REGISTER (numstore, struct_t_deserialize_red_path);
  REGISTER (numstore, txn_basic);
  REGISTER (numstore, txnt_open);
  REGISTER (numstore, txnt_merge_into);
  REGISTER (numstore, txnt_max_u_undo_lsn);
  REGISTER (numstore, txnt_min_lsn);
  REGISTER (numstore, txnt_exists);
  REGISTER (numstore, txnt_insert);
  REGISTER (numstore, txnt_get);
  REGISTER (numstore, txnt_remove);
  REGISTER (numstore, txnt_serialize);
  REGISTER (numstore, txnt_equal_ignore_state);
  REGISTER (numstore, txnt_concurrent);
  REGISTER (numstore, ta_subtype);
  REGISTER (numstore, type_accessor_builder);
  REGISTER (numstore, type_generate_string);
  REGISTER (numstore, type_malloc_copy);
  REGISTER (numstore, union_t_snprintf);
  REGISTER (numstore, union_t_byte_size);
  REGISTER (numstore, union_t_get_serial_size);
  REGISTER (numstore, union_t_serialize);
  REGISTER (numstore, union_t_deserialize_green_path);
  REGISTER (numstore, union_t_deserialize_red_path);
  REGISTER (numstore, ns_find_var_page);
  REGISTER (numstore, ns_var_get_or_create);
  REGISTER (numstore, rand_varname_same_hash);
  REGISTER (numstore, rand_varname_different_hash);
  REGISTER (numstore, wal);
  REGISTER (numstore, wal_single_entry);
  REGISTER (numstore, mem_vhmap);
  REGISTER (numstore, cgd_test_create_delete_rollback_delete);
  REGISTER (numstore, cgd_test_create_crash_close_delete);
  REGISTER (numstore, irwr_rollback_invalid_wal_header);
  REGISTER (numstore, nsdb_create_txn);
  REGISTER (numstore, nsdb_delete_txn);
  REGISTER (numstore, nsdb_insert_txn);
  REGISTER (numstore, nsdb_write_txn);
  REGISTER (numstore, nsdb_get_if_exists);
  REGISTER (numstore, numstore_to_dtype);
  REGISTER (numstore, aries_crash);
  REGISTER (numstore, smfile_data_writer);
}
